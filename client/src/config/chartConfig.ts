export const chartConfig = {
  scales: {
    x: [{
        type: 'time',
        gridLines: {
            display: false,
        },
        ticks: {
            maxRotation: 0,
            minRotation: 0,
        },
    }],
  }
};